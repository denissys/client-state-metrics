default:
  keepAliveMetrics:
    read:
      clientData:
        throttleIntervalInMs: 100
      pagination:
        count: 500
    maxHitsPerHour: 60 # One hit per minute
    health:
      checkIntervalInMs: 30000
  db:
    mongo:
      address: localhost
      port: 27001
      databaseName: metrics